# DeGhost
DeGhost is a hardware-aware Windows optimizer that cleans caches across drives, removes safe bloatware, and applies performance, startup, and latency tweaks. It auto-detects system specs, supports gaming/workstation/custom modes, backs up changes, and runs as a portable, dependency-free batch + PowerShell toolkit.
